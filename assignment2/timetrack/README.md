### Track

Track time spent on a task. Allows one task active per session.

**Usage:** `track {start [label]|stop|status|log|help}`

**To run on macOS:**
 1. Install `brew`
 2. Install GNU coreutils with `brew install coreutils`
 3. Usage as described above
